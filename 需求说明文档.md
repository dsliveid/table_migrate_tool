# SQL Server 数据库结构与数据迁移工具 (TableMigrateTool)
## 需求与规格说明书 (PRD)

---

### 1. 项目背景与目标

在日常数据库开发、测试环境部署以及系统升级维护过程中，经常需要在不同的 SQL Server 实例间同步表结构、视图定义，甚至快速迁移测试数据。现有的重型数据库管理工具（如 SSMS 导出导入向导、Redgate 等）配置繁琐、自动化程度有限，或需要依赖复杂的安装包和系统环境。

本项目的目标是**打造一款轻量级、免安装、开箱即用的 Windows 桌面端 SQL Server 表结构与数据迁移工具**。用户可以在不同实例间按需选择单库、批量筛选表与视图、灵活定制迁移策略，并且所有数据与配置完全保存在本地同级目录下，随拷随走。

---

### 2. 总体架构与设计原则

1. **便携绿色 (Portable & Green)**：
   - 采用免安装的绿色分发形式，程序双击即运行。
   - 所有运行时产生的数据（连接配置、保存的迁移方案、操作日志、历史快照等）严格存放在应用程序同级目录（如 `./data/`），不向系统注册表或用户的 `%APPDATA%` 写入不可控配置，方便随时打包、复制与迁移。
2. **直观向导式操作 (Wizard-Based Workflow)**：
   - 遵循“配置连接 -> 选库选表 -> 冲突策略 -> 确认与保存 -> 异步执行与监控”的向导步骤。
3. **稳健的冲突解决机制 (Resilient Schema Evolution)**：
   - 充分考虑重复多次迁移的场景，面对字段增删、类型变更、索引调整，特别是“主键变更无法平滑 ALTER”等复杂场景，提供自动增量比对更新和“先删后建”安全兜底方案。
4. **安全与可审计 (Audit & Safety)**：
   - 连接密码本地高强度对称加密存储（避免明文泄漏）。
   - 目标端在执行破坏性操作（如 Drop Table / Rebuild）前具备明确告警与二次确认机制，支持操作前元数据备份。

---

### 3. 详细功能需求

```mermaid
flowchart TD
    A[启动应用] --> B[数据源管理]
    B --> C[步骤1: 选择来源与目标数据源及数据库]
    C --> D[步骤2: 选择迁移对象(表/视图)与内容类型]
    D --> E[步骤3: 设置冲突与更新策略(增量更新/重建兜底)]
    E --> F[步骤4: 方案确认与方案保存/复用]
    F --> G[步骤5: 异步自动化执行]
    G --> H[输出实时日志与迁移结果报告]
```

#### 3.1 数据源配置管理 (Datasource Management)
- **多实例管理**：支持维护多个不同的 SQL Server 数据库连接配置。
- **连接属性**：
  - 连接名称（别名，如 `开发环境-192.168.1.100`、`生产镜像-10.0.0.2`）
  - 主机地址与端口（默认 `1433`，支持命名实例格式如 `HOST\SQLEXPRESS`）
  - 身份验证方式：
    - SQL Server 身份验证（账号、密码）
    - Windows 身份验证（当前登录凭据）
  - 连接参数高级配置：连接超时时间（Connection Timeout）、查询超时时间（Command Timeout）、`TrustServerCertificate`（解决证书链信任问题）
- **功能操作**：
  - 新增连接、编辑连接、删除连接。
  - **测试连接**：实时测试联通性并获取 SQL Server 版本号信息。
  - **密码安全**：敏感密码采用本地加密保存（基于机器指纹或应用内置密钥加密）。

---

#### 3.2 迁移流程配置 (Step-by-step Wizard)

##### 第一步：数据源与数据库选择
- **来源端选择**：从已配置的数据源下拉选择“来源数据源”，系统自动枚举该实例下所有在线的数据库，用户选择“来源数据库”（单选）。
- **目标端选择**：从已配置的数据源下拉选择“目标数据源”，系统自动枚举目标数据库，用户选择“目标数据库”（单选）。
  - *扩展检查*：如果目标端不存在同名库，提示用户是否需要在目标端快速执行 `CREATE DATABASE`。

##### 第二步：迁移对象与内容筛选
- **对象列表动态拉取**：选择来源库后，系统自动扫描其所有用户表（`TABLE`）和视图（`VIEW`）。
- **默认规则与全选控制**：
  - 默认全选所有表和所有视图。
  - 提供快速工具栏：一键全选、一键反选、按名称快速模糊过滤搜索、按对象类型过滤。
- **迁移内容类型（规则核心）**：
  - **普通表（Table）**：
    - 可选：**仅迁移表结构**（Schema Only）
    - 可选：**结构 + 数据**（Schema + Data）
    - 支持全局默认设置，也支持针对特定某几张表单独微调。
  - **视图（View）**：
    - **强制只迁移定义**：视图本质是虚拟表与查询定义，系统默认且强制限制其“不可勾选迁移数据”，仅提取其 `CREATE VIEW` 脚本并迁移结构。
- **高级元数据支持**：
  - 支持迁移字段类型、长度、精度、是否允许 NULL、默认值约束（Default Constraints）。
  - 支持迁移主键（Primary Key）、普通单列/复合索引（Non-Clustered Indexes）、唯一约束/唯一索引（Unique Constraint/Index）。
  - 支持迁移字段与表的扩展属性（Extended Properties，即中文列说明/注释）。
  - 外键（Foreign Key）处理：支持可选项（“忽略外键”、“先禁用导入后重建外键”）。

##### 第三步：多次迁移与表结构冲突解决策略 (Schema Evolution Strategy)
由于工具需要支持**多次、反复执行迁移**，目标库可能已经存在旧版本表，必须具备完备的冲突应对策略：

1. **策略 A：增量对比与智能更新 (Smart Alter / Diff) - [默认推荐]**
   - **比对逻辑**：比对源表与目标表的字段差异。
   - **新增字段**：自动执行 `ALTER TABLE [table] ADD [col] [type] ...`。
   - **修改字段**：若类型、长度、可空性发生变更且不涉及不可逆破坏，生成 `ALTER TABLE [table] ALTER COLUMN [col] ...`。
   - **索引同步**：若源端有新索引，在目标端创建；目标端多余索引可根据配置选择保留或清理。
2. **策略 B：表结构先删后建兜底方案 (Drop and Recreate) - [强力兜底]**
   - **适用场景**：
     - 主键定义发生变动（例如由普通主键变更为复合主键、自增主键标识变动等，普通 ALTER 语句往往会因约束报错而失败）。
     - 字段类型不兼容强转（如 `VARCHAR` 转 `INT` 失败）。
     - 用户明确希望目标端与来源端保持绝对一模一样的崭新结构。
   - **执行流程**：
     1. 先安全剔除依赖该表的外键约束。
     2. 执行 `DROP TABLE IF EXISTS [table]`。
     3. 依据来源表全新执行 `CREATE TABLE`，完整建立主键、约束和索引。
   - **数据安全处置**：
     - 若当前迁移方案包含“迁移数据”，则重建表后从来源端拉取全新全量数据，恢复最新状态。
     - 若当前迁移方案为“仅迁移结构”，系统在界面必须弹出高亮警示：“目标表历史数据将被永久清除”，并要求用户明确二次确认。
3. **策略 C：已存在则跳过 (Skip If Exists)**
   - 目标表已存在时直接略过，仅迁移目标端缺失的新表/新视图。

##### 第四步：方案确认与持久化管理 (Plan Persistence)
- **迁移方案概要预览**：
  - 汇总面板展示：来源/目标实例、源库/目标库。
  - 待迁移对象总览统计：`共选定 X 张表（Y张仅结构，Z张含数据），M 个视图`。
  - 冲突处理策略标记（如“遇冲突自动智能更新 / 遇冲突强行先删后建”）。
- **方案保存与复用 (Save / Load Plan)**：
  - **保存方案**：用户可点击“保存迁移方案”，输入方案名称（如 `财务系统模块迁移-正式到测试`），将所有勾选清单、策略、数据库对应关系保存至本地持久化存储。
  - **方案列表管理**：主界面或顶部提供“方案管理”入口，支持查看历史方案、快速一键载入方案、重命名、导出方案、删除无用方案。
  - **快速复用**：载入后直接定位到确认页，点击即可一键再次发起迁移，极大提升日常迭代效率。

##### 第五步：自动化执行与实时监控 (Execution & Monitor)
- **异步安全执行**：
  - 采用独立的后台工作线程（QThread / 异步任务），确保桌面客户端界面始终流畅响应，杜绝“未响应”白屏卡死。
  - 支持随时点击“取消/中止迁移”。
- **精细化执行控制**：
  - **自增列处理**：迁移包含自增列（IDENTITY）的数据时，自动包裹 `SET IDENTITY_INSERT [table] ON/OFF`。
  - **视图依赖与重试**：视图之间存在依赖关系（视图查视图），提取时按依赖拓扑排序，或在第一遍创建失败后自动执行重试机制。
- **实时日志与进度呈现**：
  - 总体进度条：展示当前已完成任务百分比。
  - 详细日志控制台：按时间戳打印每一步动作（`[INFO] 正在创建表 [dbo].[Users]... 成功`、`[DATA] 导入表 [dbo].[Orders] 数据: 15,200 行完成 (耗时 1.2s)`、`[WARN] 字段 [Email] 长度扩展至 varchar(200)`）。
  - 遇到单表失败时支持“跳过并继续下一张”或“立即终止”配置。
- **执行结果报告**：
  - 迁移结束后输出执行报告总结卡片（成功数、失败数、跳过数、总耗时）。
  - 支持一键导出本次执行日志（`.txt` 或 `.log` 格式）。

---

### 4. 非功能性与运行环境要求

| 项目 | 要求规范 |
| :--- | :--- |
| **操作系统** | Windows 10 / Windows 11 / Windows Server 2016+ (64-bit) |
| **部署形式** | **绿色免安装包**（解压即用或单文件执行，无需任何 Setup 安装向导，无需全局管理员权限） |
| **目标数据库版本** | 兼容 SQL Server 2008 R2 / 2012 / 2014 / 2016 / 2017 / 2019 / 2022 |
| **本地数据存储目录** | 程序同级目录 `./data/`。内部存放：<br>1. `connections.json` 或 `app.db` (SQLite，加密存储连接和方案)<br>2. `logs/` (日常运行与迁移日志)<br>无论程序被拷贝移动到哪台电脑或哪个盘符，配置随程序自闭环流动 |
| **性能指标** | 1. 结构比对提取响应时间在 500 张表以内不超过 5 秒；<br>2. 支持中大批量数据流式分块写入（Batch Insert / SqlBulkCopy），避免内存溢出； |
| **安全性** | 1. 绝不收集任何用户数据库数据；<br>2. 破坏性操作强制红色标识提示与弹窗确认；<br>3. 数据库密码 AES-256 加密保存。 |

---

### 5. 建议技术选型分析

| 模块 | 推荐技术 | 选型优势 |
| :--- | :--- | :--- |
| **GUI 界面框架** | **Python + PyQt6 / PySide6** (Qt) | 界面现代、控件丰富（TableTree、向导式布局）、响应迅速；原生多线程支持完备；跨 Windows 主题与高 DPI 缩放适配极佳。 |
| **数据库驱动与连接** | **pymssql** 或 **pyodbc** | 官方稳定驱动；直接走 TDS / ODBC 协议，无需在用户机器单独安装复杂客户端工具，支持直接执行 DDL 与流式数据查询。 |
| **本地元数据与配置持久化** | **SQLite3 (内置)** 或 **加密 JSON** | 无需额外数据库服务，自包含单一数据库文件存放在 `./data/app.db`，天然支持事务和复杂关系管理（如保存的方案、连接池信息）。 |
| **打包与绿色分发** | **PyInstaller / Nuitka** | 通过参数配置 `--onedir` 生成独立便携文件夹，将解释器、Qt依赖和驱动打包，最终输出一个免安装的独立便携分发包。 |

---

### 6. 项目里程碑规划

- **Phase 1 (核心引擎与元数据抽取)**：
  - 实现 SQL Server 元数据反射模块（获取表、字段、PK/FK、索引、视图 DDL）。
  - 实现增量比对引擎与先删后建 DDL 生成器。
  - 实现数据批量写入模块（含 Identity 处理）。
- **Phase 2 (便携存储与业务逻辑层)**：
  - 实现 `./data/` 目录自检测与初始化。
  - 实现数据源管理模块（CRUD + 密码加密）。
  - 实现迁移方案的保存、读取与删除机制。
- **Phase 3 (桌面 UI 界面与交互)**：
  - 基于 PySide6 构建现代化向导界面（连接选择、库选择、树状对象选择表、策略选项）。
  - 构建方案管理与快速复用界面。
  - 实现多线程异步执行控制与富文本日志终端。
- **Phase 4 (异常加固、测试与免安装打包)**：
  - 主键调整极端场景专项测试与覆盖。
  - 视图依赖拓扑排序与重试机制完善。
  - 制作绿色便携包（带自动化构建打包脚本），验证在无 Python 环境的纯净 Windows 机器上直接运行。
